import styled from "styled-components";

const WeatherUIViewWrapper  = styled.div`background: linear-gradient(45deg, var(--bs-primary), lightblue);
padding: 15px;
color: white;
border-radius: 10px;
margin-bottom: 10px;`;

export default WeatherUIViewWrapper;